CC="gcc"
CXX="g++"
CFLAGS="-m64 -march=native -mtune=native -O2 -pipe -fomit-frame-pointer"
CXXFLAGS="${CFLAGS}"
FCFLAGS="${CFLAGS}"
FFLAGS="${CFLAGS}"
LDFLAGS="-Wl,-O2,--sort-common,--as-needed,--hash-style=gnu"
AR="gcc-ar"
NM="gcc-nm"
RANLIB="gcc-ranlib"
